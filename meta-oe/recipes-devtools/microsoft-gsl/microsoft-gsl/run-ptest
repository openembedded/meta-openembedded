#!/bin/sh

# Upstream builds one executable per tests/*.cpp source, so iterate rather
# than naming them individually.
cd "$(dirname "$0")" || exit 1

for t in *_tests; do
    [ -x "./$t" ] || continue
    if "./$t"; then
        echo "PASS: $t"
    else
        echo "FAIL: $t"
    fi
done
